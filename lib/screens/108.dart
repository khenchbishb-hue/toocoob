import 'package:flutter/material.dart';

class Game108Page extends StatelessWidget {
  const Game108Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('108'),
        elevation: 0,
      ),
      body: const Center(
        child: Text('108'),
      ),
    );
  }
}
