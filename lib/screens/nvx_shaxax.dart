import 'package:flutter/material.dart';

class NyxShaxaxPage extends StatelessWidget {
  const NyxShaxaxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Нүх шахах'),
        elevation: 0,
      ),
      body: const Center(
        child: Text('Нүх шахах'),
      ),
    );
  }
}
