import 'package:flutter/material.dart';

class MuushigPage extends StatelessWidget {
  const MuushigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Муушиг'),
        elevation: 0,
      ),
      body: const Center(
        child: Text('Муушиг'),
      ),
    );
  }
}
