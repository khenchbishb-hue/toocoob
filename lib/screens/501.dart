import 'package:flutter/material.dart';

class Game501Page extends StatelessWidget {
  const Game501Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('501'),
        elevation: 0,
      ),
      body: const Center(
        child: Text('501'),
      ),
    );
  }
}
