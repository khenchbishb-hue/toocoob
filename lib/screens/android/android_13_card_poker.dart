// TODO: Implement this file properly. The following is a placeholder to prevent build errors.
import 'package:flutter/material.dart';

class Android13CardPokerPlaceholder extends StatelessWidget {
  const Android13CardPokerPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Android 13 Card Poker - Work in progress'),
    );
  }
}
