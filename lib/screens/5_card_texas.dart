import 'package:flutter/material.dart';

class CardTexasPage extends StatelessWidget {
  const CardTexasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('5 модны Техас'),
        elevation: 0,
      ),
      body: const Center(
        child: Text('5 модны Техас'),
      ),
    );
  }
}
